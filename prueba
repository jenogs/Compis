PROG

global int i = 0;
global int j = 0;

int function UNO (int a) beginf {
	i = a * 2;
	if(i < a+4) {
		UNO(a+1);
	}
	return(a);
} endf

int function DOS (int b) beginf {
	b = b*i+j;
	return(b*2);
} endf

begin {
	i = 2;
	j = i*2-1;

	UNO(j);

	print(i + DOS(i+j));
}
end
